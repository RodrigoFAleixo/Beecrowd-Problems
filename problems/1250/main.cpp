#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

void solve(){
    int num_casos;
    cin >> num_casos;
    
    vector<int> alturas(num_casos);
    string pulos;
    
    for (int i = 0; i < num_casos; i++)
    {
        cin >> alturas.at(i);
    }

    cin >> pulos;

    for (size_t i = 0; i < num_casos; i++)
    {
        
    }
    

    
    
}

int main(){

    int loop;

    cin >> loop;


    while (loop--)
    {
        
    }
    

    return 0;
}