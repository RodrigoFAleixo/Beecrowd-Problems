#include <bits/stdc++.h>


using namespace std;


int func(){


    
}

int main(){

    int casos, caso;
    int  nums;
    cin >> casos;
    
    for(int i = 0; i < casos; i++){
        cin >> caso;
        vector<int> v;
        for(int j = 0; j < caso; j++)
        {
            cin >> nums;
            v.push_back(nums);
        }
        
        sort(v.begin(), v.end());
        

    }

    

    return 0;
}